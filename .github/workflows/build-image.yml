name: build-image
# Build the Minister production image in the cloud and push to GHCR, so the
# Lightsail box (2 GB, can't build) just pulls. Triggers on main + manual.
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  packages: write
concurrency:
  group: build-minister-${{ github.ref }}
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/minister/Dockerfile.prod
          push: true
          tags: ghcr.io/ministryofmany/ministry-minister:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
