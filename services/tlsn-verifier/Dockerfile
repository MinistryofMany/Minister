FROM alpine
# Stage 6: Rust HTTP sidecar wrapping the `tlsn-verifier` crate. The
# Next.js app posts finalized TLSNotary presentations here and gets back a
# verified transcript.
CMD ["sh", "-c", "echo 'tlsn-verifier placeholder — see README.md' && sleep infinity"]
