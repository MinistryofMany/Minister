# ws-proxy

Placeholder. Stage 6 will host a small WebSocket → TCP relay so the Tessera browser extension can reach external HTTPS servers (id.me, etc.) during a TLSNotary proof flow. Running our own lets us control rate limits and access logs.
