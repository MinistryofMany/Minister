FROM alpine
# Stage 6: minimal WebSocket relay between the browser extension and the
# target HTTPS server (e.g. id.me). The extension can't open raw TCP
# sockets, so it tunnels through this proxy.
CMD ["sh", "-c", "echo 'ws-proxy placeholder — see README.md' && sleep infinity"]
